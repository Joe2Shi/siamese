server:
  port: 8003
spring:
  application:
    name: siamese-item-interface
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.path}
        namespace: ${nacos.namespace}
        group: ${nacos.group}
        ip: 121.43.234.186
        port: 8003
      config:
        server-addr: ${nacos.path}
        namespace: ${nacos.namespace}
        group: ${nacos.group}
        file-extension: yaml

nacos:
  path: 47.97.37.85:8848
  namespace: 5ec3460f-96bb-47d4-a660-0a0dd82900cd
  group: SIAMESE_GROUP
