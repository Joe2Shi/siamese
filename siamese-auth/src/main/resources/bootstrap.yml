server:
  port: 10010
spring:
  application:
    name: siamese-auth
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.path}
        namespace: ${nacos.namespace}
        group: ${nacos.group}
      config:
        server-addr: ${nacos.path}
        namespace: ${nacos.namespace}
        group: ${nacos.group}
        file-extension: yaml

nacos:
  path: 192.168.1.124:8848
  namespace: 5ec3460f-96bb-47d4-a660-0a0dd82900cd
  group: SIAMESE_GROUP
