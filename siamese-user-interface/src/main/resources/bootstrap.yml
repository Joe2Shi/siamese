server:
  port: 8003
spring:
  application:
    name: siamese-user-interface
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.server-address}
        namespace: ${nacos.namespace}
        group: ${nacos.group}
      config:
        server-addr: ${nacos.server-address}
        namespace: ${nacos.namespace}
        group: ${nacos.group}
        file-extension: yml

nacos:
  server-address: 192.168.1.124:8848
  namespace: fda4fd06-c23e-4981-ba13-8cdacb0bef0e
  group: SIAMESE_GROUP
